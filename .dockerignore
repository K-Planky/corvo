# Keep the build context small. The image build COPYs .mvn/, mvnw, pom.xml, src/ and the
# frontend source (built by the web stage); node_modules + a prior dist are rebuilt in-image.
target/
frontend/node_modules/
frontend/dist/
.git/
.github/
.idea/
.claude/
docs/
*.md
.env
.env.example
.env.prod
